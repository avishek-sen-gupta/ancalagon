# The contract a task answers in when its caller named none: prose in a single field.
from ancalagon.contracts.class_ref import ClassRef
from ancalagon.contracts.free_text_module import FREE_TEXT_FILE

FREE_TEXT_REF = ClassRef(module=FREE_TEXT_FILE, name="FreeText")
